# Database Configuration
# Vercel Postgres or External PostgreSQL URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?connect_timeout=15
DATABASE_URL="postgresql://username:password@host:5432/database?connect_timeout=15"

# NextAuth Configuration
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-nextauth-secret-here"

# Production URL (your Vercel deployment URL)
NEXTAUTH_URL="https://your-app.vercel.app"

# AWS Configuration (Optional - for file storage)
AWS_PROFILE="hosted_storage"
AWS_REGION="us-west-2"
AWS_BUCKET_NAME="your-bucket-name"
AWS_FOLDER_PREFIX="your-prefix/"

# Additional Configuration (if needed)
# Add any other environment variables your app needs here
